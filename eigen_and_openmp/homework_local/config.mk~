# Variables
# N = 10 50 100 200 500 1000 2000 3000 4000
OPT_LEV = 0 3
N = 100 200
SAMPLES = 5

# Outputs

DATA = data
DATA_DAT = $(DATA).dat

# Files
CPP_FILES = $(wildcard *.cpp)

MAIN = main
MAIN_OUT = $(MAIN).out

PLOT = plot
PLOT_PY = $(PLOT).py

CONFIG_MK = config.mk 

RES_1 = strong_scaling
RES_1_SH = $(RES_1).sh

RES_2 = weak_scaling
RES_2_SH = $(RES_2).sh


# Compilers
COM_CPP = g++
COM_PY = python3
STD_VER = -std=c++17

# Compiler flags
SAN_FLAG = -fsanitize=address,leak,pointer-compare,pointer-subtract,undefined
WAR_FLAG = -Wall
ADD_FLAG = -g

